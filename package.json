{
  "name": "s-browser",
  "version": "0.1.0",
  "description": "S Browser - Samsung Internet-inspired desktop browser for Windows 11 with Chrome extension support",
  "main": "src/main.js",
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "start:dev": "electron . --dev",
    "lint": "node --check src/main.js && node --check src/preload.js && node --check src/renderer/renderer.js",
    "dist:win": "electron-builder --win nsis --config electron-builder.yml",
    "dist": "electron-builder --config electron-builder.yml"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^24.13.3"
  }
}
